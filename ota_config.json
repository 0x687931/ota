{
  "_owner_comment": "GitHub username (required)",
  "owner": "YOUR_GITHUB_USERNAME",
  "_repo_comment": "Repository name (required)",
  "repo": "YOUR_REPO_NAME",
  "_ssid_comment": "WiFi SSID (required)",
  "ssid": "WIFI",
  "_password_comment": "WiFi password (required for secured networks)",
  "password": "SECRET",
  "_channel_comment": "Update channel: 'stable' or 'developer' (required)",
  "channel": "developer",
  "_branch_comment": "Branch for 'developer' channel (optional)",
  "branch": "main",
  "_token_comment": "GitHub token; use null or empty for public repos (optional)",
  "token": null,
  "_allow_comment": "List of paths allowed to update (required)",
  "allow": ["README.md"],
  "_ignore_comment": "Paths to skip during updates (optional)",
  "ignore": [],
  "_chunk_comment": "Download buffer size in bytes (optional)",
  "chunk": 512,
  "_stage_dir_comment": "Staging directory (optional)",
  "stage_dir": ".ota_stage",
  "_backup_dir_comment": "Backup directory (optional)",
  "backup_dir": ".ota_backup",
  "_connect_timeout_sec_comment": "Connect timeout in seconds (optional)",
  "connect_timeout_sec": 20,
  "_http_timeout_sec_comment": "HTTP timeout in seconds (optional)",
  "http_timeout_sec": 20,
  "_retries_comment": "Number of retries (optional)",
  "retries": 3,
  "_backoff_sec_comment": "Delay between retries in seconds (optional)",
  "backoff_sec": 3,
  "_reset_mode_comment": "Reset behavior: 'hard', 'soft', or 'none' (optional)",
  "reset_mode": "hard",
  "_force_comment": "Set true to force update even if versions match (optional)",
  "force": false,
  "_debug_comment": "Set true for verbose logging (optional)",
  "debug": false
}
